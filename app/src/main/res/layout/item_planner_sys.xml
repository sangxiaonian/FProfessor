<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"

    android:paddingTop="@dimen/app_cut_big"
    android:paddingStart="@dimen/app_margin_ends"
    android:paddingEnd="@dimen/planner_img_size"
    android:orientation="horizontal">
    
    <ImageView
        android:id="@+id/img_icon"
        android:layout_width="@dimen/planner_img_size"
        android:layout_height="@dimen/planner_img_size"
        android:src="@mipmap/icon_tcacher"
        />
    <TextView
        android:background="@drawable/icon_thedialog"
        android:id="@+id/tv_planner"
        android:textSize="@dimen/app_title_text_size"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="fasdfasdfasdfasdf"

        />
    
</LinearLayout>